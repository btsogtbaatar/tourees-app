import { StyleSheet } from 'react-native';

export const TabItemStyle = StyleSheet.create({
  tabButton: {
    paddingVertical: 16,
    borderRadius: 16,
    borderWidth: 1,
    flex: 1,
    flexDirection: 'column',
    alignItems: 'center',
  },
});
