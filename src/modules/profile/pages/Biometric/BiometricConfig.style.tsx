import { StyleSheet } from 'react-native';
import { verticalScale } from '../../../../utilities';

const BiometricConfigStyle = StyleSheet.create({
  container: {
    marginTop: verticalScale(8 ),
  },
});

export default BiometricConfigStyle;
