import { StyleSheet } from 'react-native';
import { colors } from '../../theme';

const GroupedMenuListStyle = StyleSheet.create({
  container: {
    backgroundColor: colors.white,
    justifyContent: 'space-between',
    borderRadius: 8,
  },
});
export default GroupedMenuListStyle;
