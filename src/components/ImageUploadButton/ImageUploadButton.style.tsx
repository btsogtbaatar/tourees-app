import { StyleSheet } from 'react-native';

const ImageUploadButtonStyle = StyleSheet.create({
  columnWrapperStyle: {
    justifyContent: 'space-between',
    gap: 8,
  },
  contentContainerStyle: {
    gap: 8,
  },
});

export default ImageUploadButtonStyle;
