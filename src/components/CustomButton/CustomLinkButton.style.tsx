import { StyleSheet } from 'react-native';
import { colors } from '../../theme';

export const CustomLinkButtonStyle = StyleSheet.create({
  button: {
    backgroundColor: 'transparent',
  },
  text: {
    color: colors.gray500,
  },
});
